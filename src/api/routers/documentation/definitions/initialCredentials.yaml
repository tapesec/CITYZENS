definition:
  initialCredentials:
    properties:
      access_token:
        type: string
      refresh_token:
        type: string
      id_token:
        type: string
      scope:
        type: string
      expires_in:
        type: integer
        format: timestamp
      token_type:
        type: string