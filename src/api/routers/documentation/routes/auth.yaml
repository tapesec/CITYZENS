/auth/token:
  # This is a HTTP operation
  get:
    tags:
      - Auth
    # Describe this verb here. Note: you can use markdown
    description: |
      Retourne un jeton jwt valide à l'appel avec des identifiants correctes.
    consumes:
    - application/json
    produces:
    - application/json
    # This is array of GET operation parameters:
    parameters:
      # An example parameter that is in query and is required
      -
        name: username
        in: query
        description: Nom d'utilisateur
        required: true
        type: string
      -
        name: password
        in: query
        description: Mot de passe
        required: true
        type: string
    # Expected responses for this operation:
    responses:
      # Response code
      200:
        description: Utilisateur authorisé
        content:
        schema:
          type: object
          $ref: '#/definitions/initialCredentials'